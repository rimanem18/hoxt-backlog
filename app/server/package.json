{
  "name": "server",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "check": "biome check src",
    "fix": "biome check src --write",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.9.0"
  },
  "devDependencies": {
    "@biomejs/biome": "2.1.4",
    "@types/bun": "latest"
  }
}
