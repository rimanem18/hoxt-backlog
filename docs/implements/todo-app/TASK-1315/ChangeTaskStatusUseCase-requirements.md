# ChangeTaskStatusUseCase 要件定義書

## 📄 ドキュメント情報

- **TASK-ID**: TASK-1315
- **作成日**: 2025-11-24
- **要件名**: todo-app
- **機能名**: ChangeTaskStatusUseCase
- **フェーズ**: Phase 3 - バックエンドApplication層実装

---

## 1. 機能の概要（EARS要件定義書・設計文書ベース）

### 何をする機能か

🔵 タスクのステータス（進行状況）を変更するユースケース。
タスクの状態遷移（未着手→進行中→レビュー中→完了）を管理する。

### どのような問題を解決するか

🔵 ユーザーがタスクの進捗状況を追跡・管理できるようにする。
タイトルや説明の更新とは独立して、ステータスのみを効率的に変更できる。

### 想定されるユーザー

🔵 ログイン済みの一般ユーザー（タスク所有者）

### システム内での位置づけ

🔵 Application層のユースケースとして、Presentation層から呼び出される。
ITaskRepositoryのupdateStatusメソッドを使用してInfrastructure層と連携する。

- **参照したEARS要件**: REQ-004
- **参照した設計文書**: architecture.md - Application層セクション

---

## 2. 入力・出力の仕様（EARS機能要件・TypeScript型定義ベース）

### 入力パラメータ

🔵 **ChangeTaskStatusInput**

| パラメータ | 型 | 必須 | 制約 | 説明 |
|-----------|------|------|------|------|
| userId | string (UUID) | ○ | 有効なUUID形式 | 操作を行うユーザーのID |
| taskId | string (UUID) | ○ | 有効なUUID形式 | 変更対象のタスクID |
| status | TaskStatus | ○ | 許容値のみ | 新しいステータス |

🔵 **TaskStatus許容値**（値オブジェクト `TaskStatus` より）

| 値 | 説明 |
|----|------|
| `not_started` | 未着手 |
| `in_progress` | 進行中 |
| `in_review` | レビュー中 |
| `completed` | 完了 |

### 出力値

🔵 **成功時**: 更新後の `TaskEntity`

```typescript
interface TaskEntity {
  id: string;           // UUID
  userId: string;       // UUID
  title: string;        // 1-100文字
  description: string | null;
  priority: TaskPriority;
  status: TaskStatus;   // 更新後のステータス
  createdAt: Date;
  updatedAt: Date;      // 更新時刻が反映される
}
```

🔵 **失敗時**: 例外をスロー

| 例外 | 条件 |
|------|------|
| `TaskNotFoundError` | タスクが存在しない、または他ユーザーのタスク |

### 入出力の関係性

🔵 入力されたstatusがTaskEntityのstatusフィールドに反映され、
updatedAtが更新される。

### データフロー

🔵 architecture.md のデータフローより:

1. Presentation層からChangeTaskStatusUseCaseが呼び出される
2. ITaskRepository.updateStatus()でステータス更新
3. 更新されたTaskEntityを返却

- **参照したEARS要件**: REQ-004
- **参照した設計文書**: interfaces.ts - ITaskRepository.updateStatus

---

## 3. 制約条件（EARS非機能要件・アーキテクチャ設計ベース）

### パフォーマンス要件

🟡 タスク更新APIは500ms以内にレスポンスを返す（NFR-002準拠）

### セキュリティ要件

🔵 **認証**: JWT認証必須（NFR-103）
🔵 **認可**: ユーザーは自分自身のタスクのみ変更可能（RLS: NFR-102）
🔵 **バリデーション**: サーバーサイドでZodバリデーション実施（NFR-104）

### 互換性要件

🔵 DDD + クリーンアーキテクチャパターン適用（REQ-401）

### アーキテクチャ制約

🔵 **依存性注入**: ITaskRepositoryをコンストラクタで注入
🔵 **層の責務**: ビジネスロジックはApplication層で完結

### データベース制約

🔵 statusカラムのCHECK制約: `'not_started', 'in_progress', 'in_review', 'completed'`

- **参照したEARS要件**: NFR-102, NFR-103, NFR-104, REQ-401
- **参照した設計文書**: architecture.md - セキュリティ設計セクション

---

## 4. 想定される使用例（EARSEdgeケース・データフローベース）

### 基本的な使用パターン

🔵 **正常系: ステータス変更成功**

```typescript
const useCase = new ChangeTaskStatusUseCase(taskRepository);
const result = await useCase.execute({
  userId: 'user-uuid',
  taskId: 'task-uuid',
  status: 'in_progress',
});
// result: 更新されたTaskEntity (status === 'in_progress')
```

### エッジケース

🔵 **EDGE-003: タスクが見つからない**

```typescript
// 存在しないtaskIdを指定
await useCase.execute({
  userId: 'user-uuid',
  taskId: 'non-existent-uuid',
  status: 'completed',
});
// throws TaskNotFoundError
```

🔵 **EDGE-004: 他ユーザーのタスクにアクセス**

```typescript
// RLSにより他ユーザーのタスクは取得不可 → null → TaskNotFoundError
await useCase.execute({
  userId: 'user-a-uuid',
  taskId: 'user-b-task-uuid',
  status: 'completed',
});
// throws TaskNotFoundError (RLSでフィルタされて見つからない)
```

### エラーケース

🟡 **不正なステータス値**

```typescript
// TaskStatus値オブジェクトでバリデーション
await useCase.execute({
  userId: 'user-uuid',
  taskId: 'task-uuid',
  status: 'invalid_status',
});
// throws Error (不正なステータスです: invalid_status)
```

- **参照したEARS要件**: EDGE-003, EDGE-004
- **参照した設計文書**: dataflow.md - セキュリティフロー（RLS）

---

## 5. EARS要件・設計文書との対応関係

### 参照した機能要件

| 要件ID | 内容 | 信頼性 |
|--------|------|--------|
| REQ-004 | タスクのステータス(未着手・進行中・レビュー中・完了)を変更できなければならない | 🔵 |
| REQ-401 | DDD + クリーンアーキテクチャパターンを適用しなければならない | 🔵 |

### 参照した非機能要件

| 要件ID | 内容 | 信頼性 |
|--------|------|--------|
| NFR-002 | タスク作成/更新APIは500ms以内にレスポンスを返す | 🟡 |
| NFR-102 | ユーザーは自分自身のタスクのみアクセス可能（RLS） | 🔵 |
| NFR-103 | タスク操作APIはすべてJWT認証を必須とする | 🔵 |
| NFR-104 | 入力値はサーバーサイドでZodバリデーション実施 | 🔵 |

### 参照したEdgeケース

| 要件ID | 内容 | 信頼性 |
|--------|------|--------|
| EDGE-003 | 存在しないタスクを更新・削除しようとした場合、404エラーを返す | 🟡 |
| EDGE-004 | 他ユーザーのタスクにアクセスしようとした場合、403エラーを返す | 🟡 |

### 参照した設計文書

| 文書 | 該当セクション |
|------|--------------|
| architecture.md | Application層 - ChangeTaskStatusUseCase |
| architecture.md | セキュリティ設計 - RLS Policy |
| interfaces.ts | ITaskRepository.updateStatus |
| interfaces.ts | TaskStatus型定義 |

---

## 6. 実装仕様

### ファイル配置

```
app/server/src/application/usecases/
├── ChangeTaskStatusUseCase.ts
└── __tests__/
    └── ChangeTaskStatusUseCase.test.ts
```

### インターフェース定義

```typescript
// ChangeTaskStatusUseCase.ts

import type { ITaskRepository } from '@/domain/task/ITaskRepository';
import type { TaskEntity } from '@/domain/task/TaskEntity';
import { TaskNotFoundError } from '@/domain/task/errors/TaskNotFoundError';

/**
 * ステータス変更の入力パラメータ
 */
export interface ChangeTaskStatusInput {
  userId: string;
  taskId: string;
  status: string;  // TaskStatus値
}

/**
 * タスクステータス変更ユースケース
 *
 * タスクのステータスを変更する。
 * タイトルや説明の更新とは独立して、ステータスのみを効率的に変更する。
 */
export class ChangeTaskStatusUseCase {
  constructor(private readonly taskRepository: ITaskRepository) {}

  /**
   * タスクステータスを変更する
   *
   * @param input - 変更条件（userId, taskId, status）
   * @returns 更新されたTaskEntity
   * @throws {TaskNotFoundError} タスクが見つからない場合
   */
  async execute(input: ChangeTaskStatusInput): Promise<TaskEntity> {
    const task = await this.taskRepository.updateStatus(
      input.userId,
      input.taskId,
      input.status,
    );

    if (!task) {
      throw TaskNotFoundError.forTaskId(input.taskId);
    }

    return task;
  }
}
```

### 依存関係

- **依存するインターフェース**: `ITaskRepository`
- **使用するドメインエラー**: `TaskNotFoundError`
- **返却する型**: `TaskEntity`

---

## 7. テスト観点（概要）

詳細なテストケースは `/tdd-testcases` で洗い出しを行う。

### テスト分類

| 分類 | 観点 |
|------|------|
| 正常系 | ステータスが正常に変更される |
| 正常系 | 各ステータス値への変更が可能 |
| 異常系 | タスクが見つからない場合にTaskNotFoundError |
| 異常系 | 不正なステータス値でエラー |
| モック | ITaskRepositoryをモック化 |

---

## 8. 品質判定

### 判定結果: ✅ 高品質

| 観点 | 判定 | 理由 |
|------|------|------|
| 要件の曖昧さ | なし | REQ-004で明確に定義 |
| 入出力定義 | 完全 | 既存インターフェース・値オブジェクトが利用可能 |
| 制約条件 | 明確 | TaskStatus値オブジェクトで許容値を制限 |
| 実装可能性 | 確実 | UpdateTaskUseCaseと同様のパターンで実装可能 |

### 既存実装との整合性

- `ITaskRepository.updateStatus` メソッドが既に定義済み
- `TaskStatus` 値オブジェクトが既に実装済み
- `TaskNotFoundError` が既に実装済み
- `UpdateTaskUseCase` と同様のパターンで実装可能

---

## 次のステップ

次のお勧めステップ: `/tdd-testcases` でテストケースの洗い出しを行います。
